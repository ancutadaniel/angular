import { Component } from '@angular/core';

// app component decorator @Component
@Component({
  selector: 'app-root', // create a own html tag assign a string as value and hold app root
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
})
export class AppComponent {}
